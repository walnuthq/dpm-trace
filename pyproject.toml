[project]
name = "dpm-trace"
version = "0.1.0"
description = "POC for participant-scoped Canton transaction visualization and comparison."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
dpm-trace = "dpm_trace.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
